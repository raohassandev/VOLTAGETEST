# Mosquitto dev config — anonymous access, no password file required.
# Use ONLY for local development and testing without Docker.
# Never deploy this config to production.
#
# Start with:
#   mosquitto -c deployment/mosquitto/mosquitto.dev.conf
#
listener 1883
allow_anonymous true
log_dest stdout
